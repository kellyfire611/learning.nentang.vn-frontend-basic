import { test } from "@playwright/test";

test.describe("login app đơn giản", () => {
  test.skip("thử submit rỗng và kiểm tra thông báo lỗi", async () => {});
  test.skip("đăng nhập đúng rồi đăng xuất và kiểm tra trạng thái", async () => {});
  test.skip("đăng nhập sai trước, đúng sau và kiểm tra số lượt thử", async () => {});
});